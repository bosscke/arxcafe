'use strict';

// Placeholder for the future pages router (templates / views).
// For now, pages are still served by the legacy handler in server.js.

const express = require('express');
const router = express.Router();

module.exports = router;
