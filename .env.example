# MongoDB Configuration
MONGO_DEV_URI=mongodb://127.0.0.1:27017/arxcafe
# MONGO_PROD_URI=mongodb+srv://<username>:<password>@<cluster-host>/arxcafe?retryWrites=true&w=majority

# Sessions (required in production)
# Generate a strong secret, e.g. 48 bytes hex.
SESSION_SECRET=change_me_in_production

# Gemini API Configuration
GEMINI_API_KEY=your_api_key_here
AI_ASSIST_MODEL=gemini-2.5-flash

# Stripe Configuration (required for paid access)
STRIPE_SECRET_KEY=sk_live_or_test_here
STRIPE_PUBLISHABLE_KEY=pk_live_or_test_here
STRIPE_PRICE_ID=price_123
STRIPE_WEBHOOK_SECRET=whsec_123

# Password reset email (optional)
# PUBLIC_BASE_URL=https://arxcafe.com
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM="ArxCafe <no-reply@arxcafe.com>"

# Node Environment
NODE_ENV=development
# NODE_ENV=production (for production deployments)
